// Integration tests live in `tests/` and exercise the crate as an external
// consumer would: only `pub` items are accessible. Each file here is compiled
// as a separate binary.

#[test]
fn crate_compiles() {}
